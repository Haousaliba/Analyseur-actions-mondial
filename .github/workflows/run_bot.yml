name: Analyseur Sectoriel Continu

on:
  schedule:
    - cron: '0 * * * *'  # S'exécute automatiquement TOUTES LES HEURES, 24h/24
  workflow_dispatch:     # Permet de lancer le scan manuellement quand tu veux

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code du dépôt
        uses: actions/checkout@v4  # Passer à @v4 utilise Node.js 20/24 proprement 🚀

      - name: Configurer Python
        uses: actions/setup-python@v5  # Passer à @v5 utilise Node.js 20/24 proprement 🚀
        with:
          python-version: '3.10'

      - name: Installer les bibliothèques boursières
        run: |
          pip install yfinance pandas requests lxml

      - name: Exécuter l'Analyse Mondiale
        env:
          TOKEN_TELEGRAM: ${{ secrets.TOKEN_TELEGRAM }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python bot.py
